# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
python-dotenv==1.0.1

# --- HTTP client (for Hugging Face Inference API) ---
httpx==0.27.2
requests==2.32.3

# --- Hugging Face LLM (via OpenAI-compatible router) ---
huggingface_hub==0.25.2
openai==1.51.0

# --- RAG: embeddings + vector search ---
sentence-transformers==3.1.1
faiss-cpu>=1.12.0
numpy==1.26.4

# --- Markdown parsing / chunking ---
markdown-it-py==3.0.0

# Torch is pulled in by sentence-transformers automatically.
# If pip can't resolve it on your platform, install the CPU build first:
#   pip install --index-url https://download.pytorch.org/whl/cpu torch
